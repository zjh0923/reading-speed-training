# 📋 部署步骤清单（一步步照着做）

打印或打开这个文件，每完成一步就打勾 ✅

---

## 准备工作

- [ ] **步骤 0.1**：安装 Node.js
  - 访问：https://nodejs.org/
  - 下载 LTS 版本并安装
  - 验证：打开命令行，输入 `node -v`，应该显示版本号

- [ ] **步骤 0.2**：安装 Git
  - 访问：https://git-scm.com/download/win
  - 下载并安装（全部默认选项）
  - 验证：打开命令行，输入 `git --version`，应该显示版本号

- [ ] **步骤 0.3**：注册 GitHub 账号
  - 访问：https://github.com
  - 注册账号并验证邮箱

---

## 本地准备

- [ ] **步骤 1**：打开项目文件夹
  - 在 `F:\reading-speed-training` 文件夹
  - 在地址栏输入 `cmd` 按回车（打开命令行）

- [ ] **步骤 2**：安装依赖
  ```bash
  npm install
  ```
  - 等待完成（1-3分钟）
  - 看到 `added xxx packages` 表示成功

- [ ] **步骤 3**：测试构建
  ```bash
  npm run build
  ```
  - 等待完成（30秒-1分钟）
  - 检查是否有 `dist` 文件夹生成
  - 如果有，说明成功 ✅

---

## Git 配置（首次使用）

- [ ] **步骤 4**：配置 Git 用户名和邮箱
  ```bash
  git config --global user.name "你的名字"
  git config --global user.email "你的邮箱"
  ```
  - 替换成你的真实信息

- [ ] **步骤 5**：初始化 Git
  ```bash
  git init
  ```

- [ ] **步骤 6**：添加文件
  ```bash
  git add .
  ```

- [ ] **步骤 7**：提交代码
  ```bash
  git commit -m "准备部署"
  ```

---

## 上传到 GitHub

- [ ] **步骤 8**：在 GitHub 创建仓库
  - 访问：https://github.com/new
  - 仓库名：`reading-speed-training`
  - 选择 Public（公开）
  - **不要**勾选任何初始化选项
  - 点击 "Create repository"

- [ ] **步骤 9**：复制仓库地址
  - 创建后，复制显示的地址
  - 格式：`https://github.com/你的用户名/reading-speed-training.git`

- [ ] **步骤 10**：连接远程仓库
  ```bash
  git remote add origin https://github.com/你的用户名/reading-speed-training.git
  ```
  - 替换成你复制的地址

- [ ] **步骤 11**：设置主分支
  ```bash
  git branch -M main
  ```

- [ ] **步骤 12**：推送代码
  ```bash
  git push -u origin main
  ```
  - 如果要求登录，按提示操作
  - 如果要求密码，使用 Personal Access Token（见下方说明）

---

## 部署到 Vercel

- [ ] **步骤 13**：登录 Vercel
  - 访问：https://vercel.com
  - 使用 GitHub 账号登录

- [ ] **步骤 14**：导入项目
  - 点击 "Add New Project"
  - 选择你的 `reading-speed-training` 仓库
  - 点击 "Import"

- [ ] **步骤 15**：检查配置（通常自动识别）
  - Framework: Vite ✅
  - Build Command: `npm run build` ✅
  - Output Directory: `dist` ✅
  - 如果都正确，直接点击 "Deploy"

- [ ] **步骤 16**：等待部署
  - 等待 1-2 分钟
  - 看到 "Congratulations" 表示成功

- [ ] **步骤 17**：获取链接
  - 复制显示的链接（格式：`https://你的项目名.vercel.app`）
  - 在浏览器中打开测试

---

## 验证

- [ ] **步骤 18**：测试网站
  - [ ] 页面能正常加载
  - [ ] 可以看到标题和内容
  - [ ] 可以登录
  - [ ] 图片正常显示
  - [ ] 可以开始训练

---

## ✅ 完成！

如果所有步骤都完成了，你的网站已经上线了！

---

## 🔑 Personal Access Token 创建方法（如果需要）

如果 `git push` 时要求密码：

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" > "Generate new token (classic)"
3. Note：输入 `deploy`
4. Expiration：选择 `90 days` 或 `No expiration`
5. 勾选 `repo` 选项
6. 点击 "Generate token"
7. **复制生成的 token**（只显示一次！）
8. 在命令行输入密码时，粘贴这个 token

---

## ❓ 遇到问题？

查看 `超级详细部署指南.md` 获取详细说明和问题解答。

