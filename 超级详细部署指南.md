# 🚀 超级详细部署指南（从零开始）

本指南假设你完全没有经验，会详细说明每一步。

---

## 📦 第一步：安装 Node.js（如果还没有）

### 为什么要安装 Node.js？
因为我们需要用它来构建项目。

### 如何检查是否已安装？
1. 按 `Win + R` 键
2. 输入 `cmd`，按回车
3. 在黑色窗口输入：`node -v`
4. 如果显示版本号（如 `v18.17.0`），说明已安装，跳到下一步
5. 如果显示"不是内部或外部命令"，需要安装

### 如何安装 Node.js？
1. **打开浏览器**，访问：https://nodejs.org/
2. 你会看到两个绿色按钮：
   - **LTS**（推荐，长期支持版本）
   - Current（最新版本）
3. **点击 LTS 版本的按钮**下载
4. 下载完成后，双击安装文件（.msi 文件）
5. 安装过程中：
   - 一直点"下一步"（Next）"
   - 最后点击"安装（Install）"
   - 等待安装完成
6. **重要**：安装完成后，**关闭所有命令行窗口**，重新打开一个新的命令行窗口

### 验证安装
1. 按 `Win + R`，输入 `cmd`，按回车
2. 输入：`node -v`，应该显示版本号
3. 输入：`npm -v`，应该显示版本号
4. 如果都显示版本号，说明安装成功！

---

## 📁 第二步：打开项目文件夹

### 方法 1：使用文件资源管理器
1. 打开"此电脑"或"文件资源管理器"
2. 找到你的项目文件夹：`F:\reading-speed-training`
3. 在地址栏点击一下，输入 `cmd`，按回车
   - 这样会在当前文件夹打开命令行

### 方法 2：使用命令行
1. 按 `Win + R`，输入 `cmd`，按回车
2. 输入以下命令（一行一行输入，每行按回车）：
   ```bash
   F:
   cd reading-speed-training
   ```
3. 现在你应该在项目文件夹里了

### 验证位置
在命令行输入：`dir`
你应该能看到很多文件，包括 `package.json`、`web` 文件夹等。

---

## 📥 第三步：安装项目依赖

### 什么是依赖？
项目需要的一些工具和库。

### 如何安装？
在命令行窗口输入（然后按回车）：
```bash
npm install
```

### 会发生什么？
- 会看到很多文字滚动
- 可能需要等待 1-3 分钟
- 最后会显示类似 `added 123 packages` 的信息
- 如果看到 `npm ERR!` 开头的红色错误，告诉我具体错误信息

### 安装完成后
你会看到项目文件夹里多了一个 `node_modules` 文件夹（很大，不要删除它）

---

## 🔨 第四步：测试构建

### 为什么要测试？
确保项目可以正常构建，这样部署时就不会出错。

### 如何测试？
在命令行输入：
```bash
npm run build
```

### 会发生什么？
- 会看到很多文字滚动
- 等待 30 秒到 1 分钟
- 如果成功，最后会显示类似：
  ```
  dist/index.html                    xxx bytes
  built in xxxms
  ```
- 如果失败，会显示红色错误信息

### 检查结果
1. 打开项目文件夹（`F:\reading-speed-training`）
2. 看看是否有一个新的 `dist` 文件夹
3. 如果看到了 `dist` 文件夹，说明构建成功！

### 如果构建失败
告诉我具体的错误信息，我会帮你解决。

---

## 🔐 第五步：注册 GitHub 账号（如果还没有）

### 为什么要 GitHub？
我们需要把代码放到 GitHub，然后 Vercel 才能部署。

### 如何注册？
1. 打开浏览器，访问：https://github.com
2. 点击右上角的 **"Sign up"**（注册）
3. 填写信息：
   - **Username**（用户名）：自己想一个，比如 `yourname-reading`
   - **Email**（邮箱）：你的邮箱地址
   - **Password**（密码）：设置一个密码
4. 点击 **"Create account"**（创建账号）
5. 验证邮箱（GitHub 会发邮件给你，点击邮件里的链接）

---

## 📤 第六步：安装 Git（如果还没有）

### 为什么要 Git？
Git 是用来管理代码版本的工具，我们需要用它把代码上传到 GitHub。

### 如何检查是否已安装？
在命令行输入：`git --version`
- 如果显示版本号，说明已安装，跳到下一步
- 如果显示"不是内部或外部命令"，需要安装

### 如何安装 Git？
1. 访问：https://git-scm.com/download/win
2. 会自动开始下载（如果没有，点击下载按钮）
3. 下载完成后，双击安装文件（.exe 文件）
4. 安装过程中：
   - 一直点"下一步（Next）"
   - 所有选项保持默认即可
   - 最后点击"安装（Install）"
5. 安装完成后，**关闭所有命令行窗口**，重新打开一个新的

### 验证安装
在命令行输入：`git --version`
应该显示版本号，如 `git version 2.41.0`

---

## 📝 第七步：配置 Git（首次使用需要）

### 为什么要配置？
告诉 Git 你是谁，这样上传代码时就知道是谁上传的。

### 如何配置？
在命令行输入以下两行命令（每行按回车）：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

**注意**：
- 把 `"你的名字"` 改成你的真实姓名或用户名（英文引号要保留）
- 把 `"你的邮箱"` 改成你的邮箱地址（英文引号要保留）
- 邮箱最好和 GitHub 账号的邮箱一致

**示例**：
```bash
git config --global user.name "Zhang San"
git config --global user.email "zhangsan@example.com"
```

---

## 📦 第八步：初始化 Git 仓库

### 在项目文件夹打开命令行
确保你在项目文件夹里（`F:\reading-speed-training`）

### 初始化
在命令行输入：
```bash
git init
```

### 会发生什么？
会显示：`Initialized empty Git repository in F:/reading-speed-training/.git`
说明初始化成功！

---

## ➕ 第九步：添加文件到 Git

### 添加所有文件
在命令行输入：
```bash
git add .
```

### 会发生什么？
- 不会有任何输出（这是正常的）
- 所有文件已经被添加到 Git 的暂存区

---

## 💾 第十步：提交代码

### 提交
在命令行输入：
```bash
git commit -m "准备部署"
```

### 会发生什么？
- 会显示类似：
  ```
  [main (root-commit) xxxxx] 准备部署
   xx files changed, xxxx insertions(+)
  ```
- 说明提交成功！

### 如果提示需要配置邮箱和名字
说明第七步没有完成，回去完成第七步。

---

## 🌐 第十一步：在 GitHub 创建仓库

### 步骤详解
1. **登录 GitHub**
   - 访问：https://github.com
   - 如果还没登录，点击右上角 "Sign in" 登录

2. **创建新仓库**
   - 点击右上角的 **"+"** 号
   - 选择 **"New repository"**（新建仓库）

3. **填写仓库信息**
   - **Repository name**（仓库名称）：输入 `reading-speed-training`（或你喜欢的名字）
   - **Description**（描述）：可选，比如 `阅读训练网页版`
   - **Public**（公开）：选择这个（免费）
   - **⚠️ 重要**：**不要**勾选以下选项：
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - 保持这些都不勾选！

4. **创建仓库**
   - 点击绿色的 **"Create repository"**（创建仓库）按钮

5. **复制仓库地址**
   - 创建成功后，你会看到一个页面
   - 找到类似这样的地址：
     ```
     https://github.com/你的用户名/reading-speed-training.git
     ```
   - **复制这个地址**（点击地址旁边的复制按钮，或手动复制）

---

## ⬆️ 第十二步：推送代码到 GitHub

### 连接远程仓库
在命令行输入（把地址替换成你刚才复制的地址）：
```bash
git remote add origin https://github.com/你的用户名/reading-speed-training.git
```

**示例**（替换成你的）：
```bash
git remote add origin https://github.com/zhangsan/reading-speed-training.git
```

### 设置主分支
```bash
git branch -M main
```

### 推送代码
```bash
git push -u origin main
```

### 会发生什么？
1. **第一次推送**会要求登录：
   - 可能会打开浏览器让你登录 GitHub
   - 或者要求输入用户名和密码
   - 如果要求密码，可能需要使用 **Personal Access Token**（见下方说明）

2. **如果提示输入密码**：
   - GitHub 现在不支持直接用密码
   - 需要创建 Personal Access Token：
     - 访问：https://github.com/settings/tokens
     - 点击 "Generate new token" > "Generate new token (classic)"
     - Note（备注）：输入 `deploy`（随便写）
     - Expiration（过期时间）：选择 `90 days` 或 `No expiration`
     - 勾选 `repo` 选项（会全选）
     - 点击 "Generate token"
     - **重要**：复制生成的 token（只显示一次！）
     - 在命令行输入密码时，粘贴这个 token（不是你的 GitHub 密码）

3. **推送成功**会显示：
   ```
   Enumerating objects: xx, done.
   Writing objects: 100% (xx/xx), done.
   To https://github.com/...
   * [new branch]      main -> main
   ```

### 验证
1. 刷新你的 GitHub 仓库页面
2. 你应该能看到所有代码文件了！

---

## 🚀 第十三步：在 Vercel 部署

### 注册/登录 Vercel
1. **访问 Vercel**
   - 打开浏览器，访问：https://vercel.com

2. **登录**
   - 点击右上角的 **"Sign Up"** 或 **"Log In"**
   - 选择 **"Continue with GitHub"**（使用 GitHub 登录）
   - 会跳转到 GitHub 授权页面
   - 点击 **"Authorize Vercel"**（授权 Vercel）

### 导入项目
1. **添加新项目**
   - 登录后，点击 **"Add New Project"**（添加新项目）按钮
   - 或者点击 **"New Project"**（新项目）

2. **选择仓库**
   - 在 "Import Git Repository" 列表中
   - 找到你的 `reading-speed-training` 仓库
   - 如果没看到，点击 **"Adjust GitHub App Permissions"** 调整权限
   - 找到后，点击仓库旁边的 **"Import"**（导入）按钮

### 配置项目（通常自动识别）
1. **检查配置**
   - Vercel 会自动检测到你的项目类型
   - 应该显示：
     - **Framework Preset**: `Vite` ✅
     - **Root Directory**: `./`（默认）
     - **Build Command**: `npm run build` ✅
     - **Output Directory**: `dist` ✅
     - **Install Command**: `npm install`（默认）

2. **环境变量（可选）**
   - 如果你有后端 API，可以在这里添加环境变量
   - 如果没有，**跳过这一步**

3. **部署**
   - 直接点击底部的 **"Deploy"**（部署）按钮
   - 不要修改任何配置！

### 等待部署
1. **构建过程**
   - 会显示构建日志
   - 等待 1-2 分钟
   - 你会看到类似这样的信息：
     ```
     Installing dependencies...
     Building...
     Deploying...
     ```

2. **部署成功**
   - 会显示绿色的 **"Congratulations"**（恭喜）页面
   - 或者显示 **"Deployment successful"**（部署成功）

### 获取访问链接
1. **找到链接**
   - 部署成功后，你会看到一个链接：
     ```
     https://reading-speed-training.vercel.app
     ```
     （实际链接会是：`https://你的项目名.vercel.app`）

2. **访问网站**
   - 点击这个链接
   - 或者复制链接在浏览器中打开
   - 你的网站就上线了！

---

## ✅ 完成！验证网站

### 检查功能
打开你的网站链接，测试：
1. ✅ 页面能正常加载
2. ✅ 可以看到"阅读训练"标题
3. ✅ 可以点击"登录"按钮
4. ✅ 可以输入信息登录
5. ✅ 可以看到 Day1、Day2 等训练卡片
6. ✅ 图片正常显示

### 如果一切正常
🎉 **恭喜！你的网站已经成功上线了！**

---

## 🔄 后续更新代码

以后如果你想更新网站：

1. **修改代码**（在你的编辑器里）

2. **提交更改**
   ```bash
   git add .
   git commit -m "更新说明"
   git push
   ```

3. **自动部署**
   - Vercel 会自动检测到代码更新
   - 自动重新构建和部署
   - 1-2 分钟后，网站就更新了！

---

## ❓ 常见问题详细解答

### Q1: npm install 很慢怎么办？
**A**: 
- 这是正常的，因为要下载很多文件
- 可以等待，或者使用国内镜像：
  ```bash
  npm config set registry https://registry.npmmirror.com
  ```
  然后再执行 `npm install`

### Q2: git push 时提示权限错误？
**A**: 
- 确保已登录 GitHub
- 使用 Personal Access Token 而不是密码
- 参考第十二步的说明创建 token

### Q3: Vercel 部署失败？
**A**: 
- 查看构建日志中的红色错误信息
- 确保本地 `npm run build` 能成功
- 检查 `package.json` 中是否有 `build` 脚本

### Q4: 网站打开后是空白页面？
**A**: 
- 按 `F12` 打开浏览器开发者工具
- 查看 Console（控制台）标签页的错误信息
- 告诉我具体的错误信息

### Q5: 图片不显示？
**A**: 
- 检查图片路径是否正确
- 确保 `images` 文件夹在项目根目录
- 刷新页面（按 `Ctrl + F5` 强制刷新）

### Q6: 忘记 GitHub 密码？
**A**: 
- 访问：https://github.com/login
- 点击 "Forgot password?"（忘记密码）
- 按提示重置密码

---

## 📞 需要帮助？

如果遇到问题：
1. 告诉我你在哪一步卡住了
2. 把错误信息复制给我
3. 我会帮你解决！

---

## 🎯 快速检查清单

在开始之前，确保：
- [ ] 已安装 Node.js（`node -v` 有输出）
- [ ] 已安装 Git（`git --version` 有输出）
- [ ] 有 GitHub 账号
- [ ] 在项目文件夹里打开了命令行
- [ ] 知道项目文件夹的路径

---

**准备好了吗？让我们开始吧！** 🚀

