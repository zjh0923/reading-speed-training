# 解决 npm 命令未找到的问题

## 问题原因
你的系统还没有安装 Node.js，而 npm 是 Node.js 的一部分。

---

## 🚀 解决方案：安装 Node.js

### 步骤 1：下载 Node.js

1. 访问：https://nodejs.org/zh-cn/
2. 点击绿色的 **"长期支持版 (LTS)"** 按钮下载
3. 下载 Windows 安装器（.msi 文件）

### 步骤 2：安装

1. 双击下载的 .msi 文件
2. 点击 "下一步" 直到完成
3. **重要**：安装过程中确保勾选了 "Add to PATH"（默认已勾选）

### 步骤 3：验证安装

1. **关闭当前的 PowerShell 窗口**
2. **重新打开一个新的 PowerShell 窗口**
3. 输入以下命令：

```powershell
node --version
npm --version
```

如果两个命令都显示版本号（如 `v20.11.0` 和 `10.2.4`），说明安装成功！

### 步骤 4：在项目中安装依赖

```powershell
cd D:\reading-speed-training
npm install
```

---

## 🎯 方案二：不安装 Node.js，直接部署（更简单）

如果你不想安装 Node.js，可以直接推送到 GitHub，让 Vercel 自动构建：

### 步骤 1：安装 Git（如果还没有）

1. 访问：https://git-scm.com/download/win
2. 下载并安装 Git for Windows
3. 安装完成后，重新打开 PowerShell

### 步骤 2：初始化 Git 仓库

```powershell
cd D:\reading-speed-training
git init
git add .
git commit -m "准备部署"
```

### 步骤 3：创建 GitHub 仓库并推送

1. 访问GitHub
2. 创建新仓库（例如：`reading-speed-training`）
3. **不要**勾选任何初始化选项
4. 复制仓库地址，然后在 PowerShell 执行：

```powershell
git remote add origin https://github.com/你的用户名/reading-speed-training.git
git branch -M main
git push -u origin main
```

### 步骤 4：在 Vercel 部署（自动构建）

1. 访问 https://vercel.com
2. 使用 GitHub 账号登录
3. 点击 "Add New Project"
4. 选择你的仓库
5. 点击 "Deploy"
6. **Vercel 会自动安装依赖并构建，不需要本地安装 Node.js！**

---

## ✅ 推荐方案

**如果你只是想快速部署网站**，推荐使用**方案二**：
- 不需要安装 Node.js
- 不需要本地构建
- Vercel 会自动处理一切

**如果你想本地测试和开发**，推荐使用**方案一**：
- 可以本地运行 `npm run dev` 测试
- 可以本地构建检查错误

---

## 快速检查

安装 Node.js 后，运行这些命令验证：

```powershell
node --version  # 应该显示版本号
npm --version   # 应该显示版本号
npm install     # 应该能成功安装依赖
```

如果都成功了，就可以继续部署了！🎉

