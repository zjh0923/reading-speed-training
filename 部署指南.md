# 🚀 网页版部署上线指南

## 最简单的方法：使用 Vercel（推荐，5分钟上线）

### 📋 前置要求
- 一个 GitHub 账号（免费注册：https://github.com）
- 一个 Vercel 账号（可用 GitHub 账号登录，免费：https://vercel.com）

---

## 步骤 1：准备代码（2分钟）

### 1.1 安装依赖
在项目根目录打开终端/命令行，执行：
```bash
npm install
```

如果提示找不到 npm，需要先安装 Node.js：
- 下载地址：https://nodejs.org/
- 选择 LTS 版本下载安装

### 1.2 测试构建
```bash
npm run build
```

如果成功，会看到 `dist` 文件夹生成。如果失败，检查错误信息。

### 1.3 初始化 Git 仓库（如果还没有）
```bash
git init
git add .
git commit -m "准备部署"
```

### 1.4 推送到 GitHub

1. **在 GitHub 创建新仓库**
   - 访问 https://github.com/new
   - 输入仓库名称（例如：`reading-speed-training`）
   - 选择 Public（公开）
   - **不要**勾选 "Initialize this repository with a README"
   - 点击 "Create repository"

2. **推送代码到 GitHub**
   ```bash
   git remote add origin https://github.com/你的用户名/reading-speed-training.git
   git branch -M main
   git push -u origin main
   ```
   
   如果提示需要登录，按提示操作。

---

## 步骤 2：在 Vercel 部署（3分钟）

### 2.1 访问 Vercel
- 打开 https://vercel.com
- 点击 "Sign Up" 或 "Log In"
- 选择 "Continue with GitHub"（使用 GitHub 账号登录）

### 2.2 导入项目
1. 登录后，点击 **"Add New Project"**（添加新项目）
2. 在 "Import Git Repository" 中找到你的仓库
3. 点击 **"Import"**（导入）

### 2.3 配置项目（通常自动识别）
Vercel 会自动检测到你的项目配置：
- ✅ **Framework Preset**: Vite（自动识别）
- ✅ **Build Command**: `npm run build`（已配置）
- ✅ **Output Directory**: `dist`（已配置）

**直接点击 "Deploy"（部署）即可！**

### 2.4 等待部署完成
- 等待 1-2 分钟
- 看到 "Congratulations" 表示部署成功！

### 2.5 获取访问链接
部署完成后，你会看到：
- **默认域名**：`https://你的项目名.vercel.app`
- 这个链接就是你的网站地址，可以分享给任何人！

---

## ✅ 部署完成！

现在你可以：
- ✅ 打开链接访问你的网站
- ✅ 分享链接给其他人使用
- ✅ 每次推送代码到 GitHub，Vercel 会自动重新部署

---

## 🔧 常见问题解决

### ❌ 问题 1：npm 命令找不到
**解决**：
1. 下载安装 Node.js：https://nodejs.org/
2. 选择 LTS 版本
3. 安装后重启命令行

### ❌ 问题 2：构建失败
**解决**：
```bash
# 确保已安装依赖
npm install

# 如果 vite 未安装
npm install vite --save-dev

# 再次尝试构建
npm run build
```

### ❌ 问题 3：Git 推送失败
**解决**：
- 检查 GitHub 仓库地址是否正确
- 确保已登录 GitHub
- 如果使用 HTTPS，可能需要输入 GitHub 用户名和密码（或 Personal Access Token）

### ❌ 问题 4：Vercel 部署失败
**解决**：
- 检查构建日志中的错误信息
- 确保 `package.json` 中有 `build` 脚本
- 确保 `vite.config.js` 配置正确

### ❌ 问题 5：网站打开后图片不显示
**解决**：
- 检查浏览器控制台是否有错误
- 确认图片路径是 `/images/index/xxx.png` 格式
- 如果还是不行，检查 `vite.config.js` 中的 `publicDir` 配置

---

## 🌐 自定义域名（可选）

如果你想使用自己的域名（如 `reading-speed-training.com`）：

1. 在 Vercel 项目页面，点击 **"Settings"**（设置）
2. 选择 **"Domains"**（域名）
3. 输入你的域名，点击 **"Add"**（添加）
4. 按照提示配置 DNS 记录
5. 等待 DNS 生效（通常几分钟到几小时）

---

## 📝 后续更新

每次更新代码后：

1. **提交代码**
   ```bash
   git add .
   git commit -m "更新说明"
   git push
   ```

2. **Vercel 自动部署**
   - Vercel 会自动检测到代码更新
   - 自动重新构建和部署
   - 通常 1-2 分钟完成

---

## 🎉 完成！

你的网站现在已经上线了！可以通过 Vercel 提供的链接访问。

**提示**：
- 免费版 Vercel 完全够用
- 支持自动 HTTPS
- 全球 CDN 加速
- 自动部署更新

---

## 其他部署方式（备选）

### 方式 2：Netlify
1. 访问 https://www.netlify.com
2. 使用 GitHub 登录
3. 导入项目
4. 配置：Build command: `npm run build`，Publish directory: `dist`
5. 点击 Deploy

### 方式 3：GitHub Pages
1. 构建项目：`npm run build`
2. 在 GitHub 仓库设置中启用 Pages
3. 选择 `dist` 目录作为源

---

**需要帮助？** 查看项目中的其他文档：
- `QUICK_START.md` - 快速开始
- `DEPLOY.md` - 详细部署说明
- `DEPLOY_CHECKLIST.md` - 部署检查清单

