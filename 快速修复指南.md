# 快速修复指南

## ✅ 我已经修复了 vercel.json

我移除了可能引起问题的 API 配置，因为：
- 如果还没有后端 API，这个配置会导致问题
- 网页版会自动使用本地存储（localStorage），不需要后端也能正常工作

---

## 🚀 现在需要做的 3 步

### 步骤 1：重新推送更新后的文件

在 **Git Bash**（不是 PowerShell）中执行：

```bash
cd D:\reading-speed-training
git add vercel.json package.json
git commit -m "修复部署配置"
git push
```

### 步骤 2：在 Vercel 部署

1. **访问**：https://vercel.com
2. **登录**：使用 GitHub 账号登录
3. **导入项目**：
   - 点击 "Add New Project" 或 "Import Project"
   - 搜索：`reading-speed-training`
   - 选择：`zjh0923/reading-speed-training`
   - 如果找不到，点击 "Adjust GitHub App Permissions" 授权

4. **配置项目**（通常会自动识别）：
   - Framework: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Root Directory: `./`（项目根目录）

5. **部署**：
   - 点击 "Deploy"
   - 等待 1-2 分钟

### 步骤 3：检查结果

部署完成后，你会看到：
- ✅ 部署状态：Ready
- 🌐 访问链接：`https://你的项目名.vercel.app`

---

## 🔍 如果还是有问题

### 问题 1：找不到仓库

**解决**：
- 确认 GitHub 仓库地址：https://github.com/zjh0923/reading-speed-training
- 在 Vercel 中点击 "Adjust GitHub App Permissions"
- 授权访问所有仓库或特定仓库

### 问题 2：构建失败

**查看日志**：
1. 在 Vercel 项目页面
2. 点击失败的部署记录
3. 查看 "Build Logs"
4. 找到错误信息

**常见错误**：
- `vite: command not found` → 需要确保 package.json 中有 vite（已修复）
- `Cannot find module` → 需要安装依赖（Vercel 会自动安装）

### 问题 3：网站空白或 404

**检查**：
- 确认部署状态是 "Ready"
- 检查浏览器控制台是否有错误
- 尝试访问 `https://你的项目名.vercel.app/index.html`

---

## 📋 检查清单

在推送之前，确认：
- [ ] `package.json` 中有 `vite` 依赖（已修复）
- [ ] `vercel.json` 配置正确（已修复）
- [ ] 代码已推送到 GitHub
- [ ] 在 Vercel 中能找到仓库

---

## 🎯 最简单的验证方法

1. **确认 GitHub 有代码**：
   - 访问：https://github.com/zjh0923/reading-speed-training
   - 确认能看到 `web/`、`package.json`、`vercel.json` 等文件

2. **在 Vercel 部署**：
   - 如果能找到仓库并部署成功，就完成了！

3. **测试网站**：
   - 访问 Vercel 提供的链接
   - 测试登录、训练等功能

---

## 💡 重要提示

**如果暂时没有后端 API**：
- ✅ 网页版会自动使用本地存储
- ✅ 所有功能都能正常工作
- ✅ 用户数据保存在浏览器本地
- ✅ 不需要配置 `VITE_API_BASE`

**如果以后需要后端**：
- 在 Vercel 项目设置中添加环境变量：`VITE_API_BASE`
- 重新部署即可

---

按照上面的步骤操作，应该就能成功部署了！如果还有问题，告诉我具体的错误信息。🚀

