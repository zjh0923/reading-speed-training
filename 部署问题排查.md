# 部署问题排查指南

## 当前状态

从你的操作记录看，你已经：
- ✅ 配置了 Git 用户信息
- ✅ 初始化了 Git 仓库
- ✅ 提交了代码
- ✅ 推送到 GitHub：`https://github.com/zjh0923/reading-speed-training.git`

---

## 🔍 可能的问题和解决方案

### 问题 1：Git 邮箱配置拼写错误

我注意到你在配置时有一个拼写错误：
```bash
git config --global usre.email "3040780243@qq.com"  # ❌ 拼写错误：usre
```

应该是：
```bash
git config --global user.email "3040780243@qq.com"  # ✅ 正确
```

**修复方法**（在 Git Bash 中执行）：
```bash
git config --global user.email "3040780243@qq.com"
git config --global user.name "zjh0923"
```

---

### 问题 2：在 Vercel 部署时找不到项目

**检查步骤**：

1. **确认 GitHub 仓库有代码**
   - 访问：https://github.com/zjh0923/reading-speed-training
   - 检查是否能看到你的代码文件

2. **在 Vercel 中导入项目**
   - 访问：https://vercel.com
   - 登录（使用 GitHub 账号）
   - 点击 "Add New Project"
   - 在 "Import Git Repository" 中搜索 `reading-speed-training`
   - 如果找不到，点击 "Adjust GitHub App Permissions" 授权访问仓库

3. **配置项目设置**
   - Framework Preset: **Vite**（应该自动识别）
   - Root Directory: **`./`**（项目根目录）
   - Build Command: **`npm run build`**（已配置）
   - Output Directory: **`dist`**（已配置）

---

### 问题 3：Vercel 构建失败

如果构建失败，常见原因：

#### A. 缺少 vite 依赖

**解决方案**：确保 `package.json` 中有 vite

我已经帮你更新了 `package.json`，但需要确保 GitHub 上的版本是最新的。

**重新推送**（在 Git Bash 中）：
```bash
cd D:\reading-speed-training
git add package.json
git commit -m "添加 vite 依赖"
git push
```

#### B. API 地址配置问题

如果你的 `vercel.json` 中配置了 API 地址，但还没有后端，需要修改：

**选项 1：移除 API 配置（推荐）**
修改 `vercel.json`，移除 `env` 部分：
```json
{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
```

**选项 2：在 Vercel 环境变量中配置**
- 在 Vercel 项目设置中
- 进入 "Environment Variables"
- 添加 `VITE_API_BASE`（如果暂时没有后端，可以留空或删除）

---

### 问题 4：部署后网站无法访问

**检查清单**：

1. **部署状态**
   - 在 Vercel 项目页面查看部署状态
   - 如果是 "Ready"，说明部署成功
   - 点击部署记录，查看构建日志

2. **访问链接**
   - Vercel 会提供默认域名：`https://你的项目名.vercel.app`
   - 点击链接测试是否能打开

3. **常见错误**
   - **404 错误**：检查路由配置
   - **空白页面**：检查浏览器控制台错误
   - **图片不显示**：检查图片路径配置

---

## 🚀 完整部署步骤（重新操作）

### 步骤 1：确保代码已推送

在 Git Bash 中检查：
```bash
cd D:\reading-speed-training
git status
git log --oneline -3
```

如果看到最新的提交，说明代码已推送。

### 步骤 2：修复 Git 配置（如果需要）

```bash
git config --global user.email "3040780243@qq.com"
git config --global user.name "zjh0923"
```

### 步骤 3：确保 package.json 是最新的

检查 `package.json` 是否包含 vite：
```json
{
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
```

如果没有，重新推送：
```bash
git add package.json
git commit -m "更新依赖配置"
git push
```

### 步骤 4：在 Vercel 部署

1. 访问 https://vercel.com
2. 登录（GitHub 账号）
3. 点击 "Add New Project"
4. 搜索并选择 `zjh0923/reading-speed-training`
5. 配置：
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. 点击 "Deploy"
7. 等待 1-2 分钟

### 步骤 5：检查部署结果

- 查看构建日志，确认没有错误
- 访问提供的链接测试网站
- 检查所有功能是否正常

---

## 🔧 如果还是不行

### 方法 1：查看 Vercel 构建日志

1. 在 Vercel 项目页面
2. 点击失败的部署记录
3. 查看 "Build Logs"
4. 找到错误信息
5. 根据错误信息修复

### 方法 2：本地测试构建

如果安装了 Node.js，可以本地测试：
```bash
npm install
npm run build
```

如果本地构建成功，说明代码没问题，可能是 Vercel 配置问题。

### 方法 3：简化 vercel.json

如果 `vercel.json` 配置有问题，可以简化为：
```json
{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
```

---

## 📞 需要帮助？

如果你能告诉我：
1. 具体在哪一步失败了？
2. 错误信息是什么？
3. Vercel 构建日志显示什么？

我可以提供更精确的解决方案！

